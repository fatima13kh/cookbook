<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Foods</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Your Foods</h1>
    <a href="/users/<%=user._id%>/foods/new">Add a Food Item</a>
    <!-- Add the following: -->
     </div>
  <ul>
    <% foods.forEach((food)=>{ %>
    <a href="/users/<%= user._id %>/foods/<%= food._id %>">
      <li><%= food.name %></li>
    </a>
    <% }) %>
  </ul>
</div>
  </body>
</html>
